pugixml (MIT) — XPath + DOM for APE-style Naming on native.

Place official sources here:
  pugixml.hpp
  pugixml.cpp   (required for linking; ~300KB)
  pugiconfig.hpp (already in repo)

Fetch v1.14:
  ./vendor_pugixml.sh

Or from a release zip: https://github.com/zeux/pugixml/releases
